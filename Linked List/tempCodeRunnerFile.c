list_poly->coeff=temp->coeff+ptr->coeff;
        // list_poly->pow=temp->pow;
        // prev_poly=list_poly;
        // new_poly=(struct node*)malloc(sizeof(struct node));
        // new_poly->next=NULL;
        // list_poly->next=new_poly;
        // list_poly=list_poly->next;